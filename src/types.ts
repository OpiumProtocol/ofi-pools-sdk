// Blockchain
export const enum EBlockchainEnvironment {
  BINANCE = 'BINANCE',
  ETHEREUM = 'ETHEREUM',
  MATIC = 'MATIC',
  ARBITRUM = 'ARBITRUM'
}

// Pools
export type TLocalDBPool = {
  title: string
  address: string
}
